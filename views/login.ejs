<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login | Clique</title>
    <link rel="stylesheet" href="css/style.css" />
    <link rel="shortcut icon" type="image/jpg" href="assets/logo.svg"/>
    <style>
        .or-prompt {
            position: relative;
            text-decoration: none;
        }
        .or-prompt::before {
            content: "";
            position: absolute;
            display: block;
            width: 100%;
            height: 1.5px;
            bottom: 0;
            left: 0;
            background: linear-gradient(221.79deg, #00BDE3 13.62%, #0082D9 93.13%);
            transform: scaleX(0);
            transition: transform 0.04s ease-in;
        }
        .or-prompt:hover::before {
            transform: scaleX(1);
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div data-orientation="horizontal" role="separator" class="gVAEAh"></div>
    <div class="signin-box">
        <img
            id="sigin-box__user-avatar"
            src="assets/login/user-avatar-lines.svg"
            alt="User Avatar"
        />
        <div class="font-weight-600" id="signin-box__signin-text">
            Sign In
        </div>
        <div id="signin-box__signup">
            <span class="font-weight-300">New here ? </span
            ><span class="font-weight-600 or-prompt" style="cursor: pointer;" onclick="(function() {
                window.location.href = '/register'
            })();">Sign Up</span>
        </div>
        <form method="POST" action="/login" class="signin_form">
            <% if (messages.error) { %>
                <div style="align-self: center; margin-top: 0.75rem; color: white;"><%= messages.error %></div>
            <% } %>
            <input
                class="signin_form-username"
                type="email"
                id="email"
                name="email"
                placeholder="Email Address"
                spellcheck="false"
                <% if (messages.error) { %>
                    style="margin-top: 0.75rem;"
                <% } %>
            />
            <input
                type="password"
                id="password"
                name="password"
                placeholder="Password"
            />
            <input
                class="signin_form-button"
                id="submit"
                type="submit"
                value="Sign In"
            />
        </form>
    </div>
</body>
</html>
